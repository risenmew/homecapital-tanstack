export const metadataQuery = `*[_type=="agency"]{name, logo}[0]`;

export const agencyQuery = `*[_type=="agency"][0]`;

export const landingQuery = `*[_type=="landing"][0]`;

export const aboutQuery = `*[_type=="about"][0]`;
